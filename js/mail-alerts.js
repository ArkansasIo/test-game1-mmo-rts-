(function(){'use strict';var lastSeen=Number(window.localStorage&&localStorage.getItem('uc_last_mail_id')||0),timer=null;function esc(value){var d=document.createElement('div');d.textContent=String(value||'');return d.innerHTML;}function ensure(){var badge=document.getElementById('mail-alert-badge');if(!badge){badge=document.createElement('button');badge.id='mail-alert-badge';badge.type='button';badge.className='mail-alert-badge';badge.setAttribute('aria-label','Open unread game mail');badge.addEventListener('click',function(){if(typeof sendData==='function')sendData('email','get','mainDisplay','inbox');});document.body.appendChild(badge);}return badge;}function popup(latest){var box=document.getElementById('mail-alert-popup');if(!box){box=document.createElement('div');box.id='mail-alert-popup';box.className='mail-alert-popup';document.body.appendChild(box);}box.innerHTML='<strong>New game mail</strong><span>'+esc(latest.subject)+'</span><button type="button">Open Email Network</button>';box.querySelector('button').onclick=function(){box.classList.remove('visible');if(typeof sendData==='function')sendData('email','get','mainDisplay','inbox');};box.classList.add('visible');window.setTimeout(function(){box.classList.remove('visible');},9000);}function poll(){fetch('modules/mail_status.php?time='+Date.now(),{credentials:'same-origin',headers:{Accept:'application/json'}}).then(function(r){return r.ok?r.json():null;}).then(function(data){if(!data||!data.ok)return;var badge=ensure();badge.textContent=data.unread>0?'✉ '+data.unread:'✉';badge.classList.toggle('has-mail',data.unread>0);if(data.latest&&Number(data.latest.email_id)>lastSeen){lastSeen=Number(data.latest.email_id);try{localStorage.setItem('uc_last_mail_id',String(lastSeen));}catch(e){}popup(data.latest);if(window.UCGameAudio&&typeof window.UCGameAudio.route==='function')window.UCGameAudio.route('email','mainDisplay','inbox');}}).catch(function(){});}window.UCMailAlerts={poll:poll};document.addEventListener('DOMContentLoaded',function(){ensure();poll();timer=window.setInterval(poll,20000);});})();
