Action-resolution verification, 2026-08-17:

A Bombard order was uploaded through the live tactical console and the next round was resolved through the existing round control. Both client-side controls returned successful click results, confirming the expanded order links use the established AJAX request path and the new order type is accepted by the persistent combat queue.
